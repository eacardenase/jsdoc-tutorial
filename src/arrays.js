//@ts-check

/**
 * Just a simple array
 * @type {Array}
 */
const myArray = [19, 20, 30, 26, 24, {}, true, 'false'];

/**
 * Array with scores
 * @type {Array<number>}
 */
const scores = [10, 199.99, 200, 15, 12];

/**
 * Array with numbers and strings
 * @type {Array<string|number>}
 */
const scoresAndStrings = [10, 20, '30', 'false'];

/**
 * Array of names
 * @type {Array<string>}
 */
const names = ['Edwin', 'Ana', 'Keyla'];
